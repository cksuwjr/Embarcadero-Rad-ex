//---------------------------------------------------------------------------

#include <vcl.h>
#pragma hdrstop

#include "Main.h"
#include "var.h"

//---------------------------------------------------------------------------
#pragma package(smart_init)
#pragma link "cyBaseLed"
#pragma link "cyLed"
#pragma resource "*.dfm"
TForm1 *Form1;
//---------------------------------------------------------------------------

bool Input_Data[MAX_INPUT_COUNT];
bool Output_Data[MAX_OUTPUT_COUNT];


__fastcall TForm1::TForm1(TComponent* Owner)
	: TForm(Owner)
{
}
//---------------------------------------------------------------------------
void __fastcall TForm1::TimerTimer(TObject *Sender)
{
	Timer->Enabled = false;


	// 스위치 입출력


	Input_Check();

	/*
	for(int i = 0; i < MAX_INPUT_COUNT; i++)
	{
		TcyLed* Led = (TcyLed *)FindComponent("InputLed" + IntToStr(i));
		if(Led != NULL) Led->LedValue = Input_Data[i];
	}

	for(int i = 0; i < MAX_OUTPUT_COUNT; i++)
	{
		TcyLed* Led = (TcyLed *)FindComponent("OutputLed" + IntToStr(i));
		if(Led != NULL) Led->LedValue = Output_Data[i];
	}

    */

	AnsiString InLog, OutLog;

	for(int i = 0; i < MAX_INPUT_COUNT; i++)
		InLog += Input_Data[i] ? "1" : "0";

	/*
	for(int i = 0; i < MAX_OUTPUT_COUNT; i++)
		OutLog += Output_Data[i] ? "1" : "0";

	*/

	OutputDebugStringA(("IN : " + InLog).c_str());
	//OutputDebugStringA(("OUT: " + OutLog).c_str());



	Timer->Enabled = true;
}
//---------------------------------------------------------------------------

